<!DOCTYPE html>
<html lang="en">
<head>
  <title>Christmas Lists</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= render "layouts/google_analytics" if Rails.env.production? %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class='navbar navbar-inverse navbar-static-top' role='navigation'>
    <div class='container'>
      <div class='navbar-header'>
        <button type='button' class='navbar-toggle' data-toggle='collapse' data-target='.navbar-collapse'>
          <span class="sr-only">Menu</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to 'Christmas Lists', root_url, class: 'navbar-brand' %>
      </div>


      <div class='collapse navbar-collapse'>
        <ul class='nav navbar-nav'>
          <li class="divider"></li>
          <li>
             <%= link_to 'Groups', groups_path %>
          </li>
          <li class="divider"></li>
          <li>
            <%= link_to 'New List', new_collection_list_path(@collection) if params[:controller] == "collections" and params[:action] == "show" %>
            <%= link_to 'New Group', new_group_path if params[:controller] == "groups" and params[:action] == "index" %>
          </li>
        </ul>
        <ul class='nav navbar-nav navbar-right'>
          <% if current_user.provider == 'identity' %>
            <li class="divider"></li>
            <li>
              <%= link_to 'Change Password', edit_user_path(current_user) %>
            </li>
          <% end %>
          <li class="divider"></li>
          <li>
            <%= link_to "Logout #{current_user.name}", logout_path %>
          </li>
        </ul>
      </div>
    </div>
  </div>

  <div class='container'>
    <%= render partial: "shared/flash_messages", flash: flash %> 
    <div class='row'>
      <%= yield %>
    </div>
  </div>
</body>
</html>
