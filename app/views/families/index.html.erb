<h1>Families#index</h1>
<p>Find me in app/views/families/index.html.erb</p>


<% if @families.empty? %>
  You don't have any families! Create one or join one!
<% else %>
  Familes:
  <% @families.each do |family| %>
    name: <%= family.name %>
  <% end %>
<% end %>

<%= link_to 'New Family', new_family_path %>
<%= link_to 'Join Family' %>
